import React from 'react';
import ContentClient from './ContentClient';

export const metadata = {
  title: 'CMS Quản lý nội dung | Admin',
};

export default function Page() {
  return <ContentClient />;
}