import React from "react";
import CreateBrandClient from "./CreateBrandClient";

export const metadata = {
  title: "Thêm thương hiệu mới | Admin Portal",
};

export default function CreateBrandPage() {
  return <CreateBrandClient />;
}