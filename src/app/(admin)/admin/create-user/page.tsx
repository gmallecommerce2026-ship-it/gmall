import React from 'react';
import CreateUserClient from './CreateUserClient';

export const metadata = {
  title: 'Thêm người dùng mới | Admin Portal',
};

export default function CreateUserPage() {
  return <CreateUserClient />;
}